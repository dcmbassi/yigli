
export const editFormReducer = (state, action) => {
    switch (action.type) {
        case 'CHANGE_INPUT':
            
            break;
        case 'ADD_TO_ARRAY':

            break;
        case 'REMOVE_FROM_ARRAY':

            break;
        default:
            return state
    }
}